name: Build and Push Docker Image to ACR

on:
  push:
    branches:
      - main
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Docker login to ACR
      run: |
        docker login acrdockeraulas123.azurecr.io \
          -u ${{ secrets.ACR_USERNAME }} \
          -p ${{ secrets.ACR_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t acrdockeraulas123.azurecr.io/nginx-aula:latest .

    - name: Push Docker image
      run: |
        docker push acrdockeraulas123.azurecr.io/nginx-aula:latest
